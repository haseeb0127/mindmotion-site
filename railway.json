{
  "$schema": "https://railway.app/gh-pages-config-schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "runtimeConfig": {
      "aptPkgs": ["ffmpeg"]
    }
  }
}
